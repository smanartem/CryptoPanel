<resources>
    <!-- Base application theme. -->
    <style name="Theme.CryptoPanel" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pureBlue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200_dark</item>
        <item name="colorSecondaryVariant">@color/blue</item>
        <item name="buttonBarPositiveButtonStyle">@color/pureBlue</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/pureBlue</item>
        <!-- Customize your theme here. -->
        <item name="tabBackground">@color/pureBlue</item>
        <item name="android:textColor">@color/whiteShade</item>
        <item name="recyclerViewStyle">@style/recycler_style</item>
    </style>
</resources>